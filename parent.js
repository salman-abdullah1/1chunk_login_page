// Handle Teacher Registration
function handleTeacherSignup() {
    var teacherCode = document.getElementById("teacherCode").value;
    var teacherPassword = document.getElementById("teacherPassword").value;
    var teacherRePassword = document.getElementById("teacherRePassword").value;

    // Perform signup logic based on code, password, and re-password
    // Redirect to the appropriate page for teacher
    console.log("Teacher Signup Logic for: " + teacherCode);

    // Prevent form submission for demonstration
    return false;
}
